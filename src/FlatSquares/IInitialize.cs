namespace FlatSquares
{
	/// <summary>
	/// Define initialize behavior.
	/// </summary>
	public interface IInitialize
	{
		/// <summary>
		/// Initialize this instance with the specified paremeter.
		/// </summary>
		/// <param name="paremeter">Paremeter.</param>
		void Initialize(object paremeter);
	}
}
